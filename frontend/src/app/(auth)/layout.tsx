import * as React from "react";

/**
 * Auth route group — full-bleed shell; pages compose `AuthLayout`.
 */
export default function AuthGroupLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
