/** Sidebar width tokens (px) — see docs/components/layout.md for rationale. */
export const SIDEBAR_WIDTH_EXPANDED = 280;
export const SIDEBAR_WIDTH_COLLAPSED = 80;

/** Off-canvas mobile drawer width — slightly narrower than the desktop rail. */
export const SIDEBAR_DRAWER_WIDTH = 288;
