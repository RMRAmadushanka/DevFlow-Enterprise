export { QuickDialog } from "./dialog";
export type { QuickDialogProps } from "./types";

/** Re-export the ui Dialog primitives for composition when needed. */
export {
  Dialog,
  DialogTrigger,
  DialogContent,
  DialogHeader,
  DialogFooter,
  DialogTitle,
  DialogDescription,
  DialogClose,
} from "@/components/ui/dialog";
