/**
 * Pagination lives in the data-display system (tables/lists). Re-exported
 * here so navigation consumers can import from one domain barrel.
 */
export { Pagination, getPageRange } from "@/components/data-display/pagination";
export type { PaginationProps } from "@/components/data-display/pagination";
